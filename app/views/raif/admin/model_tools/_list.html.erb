<div class="card mb-4">
  <div class="card-header">
    <h5 class="mb-0"><%= t("raif.admin.common.available_tools") %></h5>
  </div>
  <div class="card-body">
    <% if model_tools.blank? %>
      <%= t("raif.admin.common.none") %>
    <% else %>
      <div class="accordion" id="modelToolsAccordion">
        <% model_tools.each_with_index do |(tool_name, tool_class), index| %>
          <% tool_locals = { tool_name: tool_name, tool_class: tool_class, index: index } %>
          <% tool_locals[:invocation_count] = local_assigns[:invocation_counts][tool_class.name] || 0 if local_assigns[:invocation_counts] %>
          <%= render partial: "raif/admin/model_tools/model_tool", locals: tool_locals %>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
