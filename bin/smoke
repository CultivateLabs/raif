#!/usr/bin/env sh
#
# Smoke-test live models against the capabilities claimed in model_manifest/*.rb.
#
# Usage:
#   bin/smoke <selectors...> [options]

set -eu

bundle exec spec/dummy/bin/rails runner script/smoke.rb "$@"
